# P1 阶段 21/22/23/24/25/26/27 验收记录

更新时间：2026-08-21

本文件记录 `docs/product.md` 中第二十一阶段“创作与长文阅读增强”至第二十七阶段“相关阅读增强”的完成定义（Definition of Done）。它不替代产品基线，也不把缺少外部基础设施的项目写成已通过。

## 当前权威基线

- Git 基线 HEAD：`4d206aa9991f52698a04f56bb38bbeb574eb8d59`；阶段 21–27 位于尚未提交的工作区修改中。
- Alembic head：`20260821_0006`；`0006` 增加不可变 Post Revision 快照表。
- 后端：`102/102` pytest 通过；compileall 与 `verify_static.py` 通过。
- 前端：`67/67` Node 回归、ESLint、生产构建和包体预算通过。
- 当前完整命令记录见 `docs/backend/VALIDATION.md`；前端逐阶段记录见 `docs/frontend/INTEGRATION_BASELINE.md`。

## 第二十一阶段：创作与长文阅读增强 DoD

| 验收域 | 完成定义 | 状态 |
| --- | --- | --- |
| Markdown 完整性 | 标题、引用、无序/有序列表、代码、表格、脚注、行内/块公式的工具栏与快捷键有真实实现；重复触发、选区和窄屏溢出有回归保护 | 通过 |
| 安全渲染 | 预览与保存后正文共用安全渲染链；脚注/公式只开放最小标签和属性，危险链接、事件属性和脚本被清理 | 通过 |
| 草稿自动保存 | 仅在真实变更后创建/保存；自动、手动、发布前保存串行；失败或撤权时本地正文不丢失 | 通过 |
| 并发保护 | Post 持久化 `edit_version`；旧版本返回 409，数据库 version column 继续兜底真实并发，旧窗口不能覆盖最新正文 | 通过 |
| Migration | `0002 → 0003` 对既有 Post 回填版本 1，支持 downgrade 后再 upgrade，约束与模型一致 | 通过 |
| 长文阅读 | Article 具有稳定 Outline/标题 ID、响应式目录、阅读进度和安全按需代码高亮；Note 不添加这些 Article 专属 UI | 通过 |
| 浏览器专项 | 工具栏、双标签冲突、脚注、公式、目录/Hash、主题、390px 溢出和 Console 已按各工作流记录完成 | 通过；详见前端基线 |

## 第二十二阶段：邮箱可信与账号恢复 DoD

| 验收域 | 完成定义 | 状态 |
| --- | --- | --- |
| 产品兼容 | 邀请码注册成功即为正常成员；未验证邮箱不阻断登录、阅读、写作和成员能力，验证只建立可信恢复通道 | 通过 |
| 邮箱验证 | 注册创建验证令牌并尝试投递；登录成员可重发；确认成功写入 `email_verified_at`，同用途旧令牌失效 | 通过 |
| 密码恢复 | 仅 active 且邮箱已验证的账号会收到重置邮件；未知、未验证、受限、冷却路径与有效路径统一返回 202 | 通过 |
| 令牌安全 | 原始令牌只在 URL fragment 和邮件中出现；数据库只保存 HMAC 摘要，并校验用途、邮箱快照、过期、撤销和单次消费；Query 令牌主动清除，页面与 Shell 使用 `no-referrer` | 通过 |
| 密码与会话 | 新密码沿用 8–128 字符策略；重置成功后消费令牌、撤销其他重置令牌和全部 Refresh Session、清除 Refresh Cookie，并通知其他标签页立即清理本地会话 | 通过 |
| 失败边界 | 邮件发送在账户事务提交后执行；投递失败不回滚注册，失败令牌被撤销，用户可以立即重新申请 | 通过 |
| 日志与邮件适配 | Console 和异常日志不记录完整邮箱、完整链接或原始令牌；SMTP adapter 固定发件人并覆盖 timeout、STARTTLS 和认证 | 通过（仓库内） |
| 前端闭环 | `/forgot-password`、`/verify-email`、`/reset-password` 均为公开路由；登录、注册、设置页有真实入口和状态反馈 | 通过（自动化与构建） |
| 浏览器验收 | 三个公开页面、fragment 清理、浅/深色、移动端与 Console 的最终实测 | 已尝试；当前桌面本地导航安全策略与服务启动权限阻断，未计通过 |

## 第二十三阶段：内容浏览完整化 DoD

| 验收域 | 完成定义 | 状态 |
| --- | --- | --- |
| 列表筛选 | Article 支持作者、Category、Tag、Collection、发布/更新排序；Note 不出现 Category，并按发生时间语义排序 | 通过 |
| URL 与分页 | 筛选、排序、页码可刷新和分享；条件变化回到第 1 页，结果收缩时回退有效页码 | 通过 |
| 卡片信息 | Article 和 Note 使用各自的信息结构；统一补齐作者、时间、分类/标签/合集、地点/心情和阅读时间 | 通过 |
| Note 影像 | 显式封面优先；无封面时批量选择第一张有效图片或 Live Photo 静态图，不使用隐藏/删除媒体 | 通过 |
| 详情信息 | Article 明确发布、更新和阅读时间；Note 明确记录时间与发布时间，并保留媒体、地点、心情、标签和合集 | 通过 |
| Archive | 年/月与作者、Category、Tag、Collection 可组合查询；结果、Facet、总数统一应用 ACL 和 semantic time | 通过 |
| 跨页面一致性 | Home、Search、Archive、Category、Tag、Collection、Profile、Favorite 共用公开浏览序列化 | 通过 |
| 浏览器验收 | 桌面与 390px 窄屏实测列表、组合筛选 URL、Archive、Article/Note 详情；无横向溢出和 Console 警告 | 通过 |

## 第二十四阶段：内容版本历史与恢复 DoD

| 验收域 | 完成定义 | 状态 |
| --- | --- | --- |
| 版本创建 | 已发布或曾发布 Post 的真实内容/分类/标签/合集变更保存修改前快照；草稿自动保存不产生 Revision，无变化保存不制造空版本 | 通过 |
| 快照范围 | 保留 Article/Note 内容字段、封面、Category、Tags、Collection 关联、可见性、发生时间、地点、心情、外部视频和 Article Slug | 通过 |
| 私密读取 | 版本列表和详情仅作者可访问；历史正文继续经过安全 Markdown 渲染，当前仍有效的绑定媒体可在作者预览中读取 | 通过 |
| 安全恢复 | 恢复要求当前 `expected_version`，过期窗口返回 409；恢复前自动把当前状态保存为新 Revision，既有历史不删除 | 通过 |
| 关联降级 | 已失效 Category/Tag/封面被安全清除或跳过；不可访问 Collection 不泄露名称，恢复时脱离合集并回退为 private | 通过 |
| Slug 连续性 | Article 恢复历史 Slug 时继续使用现有唯一性和重定向机制；被占用 Slug 明确返回冲突，不静默覆盖 | 通过 |
| 作者界面 | “我的内容”对曾发布内容提供版本历史入口；时间线支持分页、历史预览、变更摘要、恢复确认、冲突与降级提示 | 通过 |
| Migration | `0006` 可创建、降级和重新升级 Revision 表，且不损坏既有 Post；Schema/Model 保持一致 | 通过 |

## 第二十五阶段：往年今日 DoD

| 验收域 | 完成定义 | 状态 |
| --- | --- | --- |
| 时间语义 | 仅匹配往年同月同日；Article 使用 `published_at`，Note 使用 `occurred_at ?? published_at`，当前年份不进入回忆 | 通过 |
| 权限边界 | 候选、总数、年份 Facet 和分页均先应用当前 Post/Collection ACL；无权合集的名称、数量和存在性不泄漏 | 通过 |
| 内容范围 | 可读 published 与 archived 内容均可回顾；草稿、删除、隐藏、审核异常和当前年份内容不进入结果 | 通过 |
| 稳定排序 | 按记忆年份从近到远、语义时间和 Post ID 稳定排序；支持服务端分页和越界页前端回退 | 通过 |
| 首页入口 | `/home` 返回最多 4 条 ACL 安全预览和总数；有内容显示记忆卡片，无内容显示克制入口 | 通过 |
| 独立页面 | `/on-this-day` 按年份组织 Article/Note 混合卡片，展示距今年数、年份摘要、空状态和 Archive 入口 | 通过 |
| 范围控制 | 不发送通知、不生成推荐、不建设 Feed、排行榜或自动社交提醒 | 通过 |

## 第二十六阶段：Explore 朋友内容漫游 DoD

| 验收域 | 完成定义 | 状态 |
| --- | --- | --- |
| 聚合内容 | 单一成员接口返回随机 Article、随机 Note、精选 Collection、往年今日、Tag 漫游和最近成员 | 通过 |
| ACL 优先 | Post 候选、Tag 可见计数和精选 Collection 在抽取前执行当前访问者 ACL；无权内容不参与抽样，也不影响返回数量 | 通过 |
| 稳定随机 | 默认以 UTC 日期作为每日 seed；显式 URL-safe seed 可分享和重复，`换一批`只生成新 seed，不依赖热度或用户画像 | 通过 |
| 类型边界 | 随机 Post 只使用当前 published 内容；往年今日可继续包含有权 archived 内容；停用成员不进入最近成员 | 通过 |
| 隐私边界 | 最近成员只返回公开资料；不返回邮箱、状态、加入时间、登录时间或其他账户字段 | 通过 |
| 页面体验 | `/explore` 提供六个独立区块、主导航入口、各自空状态、受保护媒体和窄屏单列布局 | 通过 |
| 范围控制 | 不使用阅读、点赞、收藏或贡献数据排序；不建设热门榜、创作者排名、Feed 或个性化推荐 | 通过 |

## 第二十七阶段：相关阅读增强 DoD

| 验收域 | 完成定义 | 状态 |
| --- | --- | --- |
| 页面范围 | 仅 Article 详情展示相关阅读；最多 4 篇，不足不补，无结果隐藏整个区块 | 通过 |
| ACL 优先 | 候选形成前应用统一可读谓词；排除当前文章、草稿、隐藏、删除及无权 Collection 内容 | 通过 |
| 静态关系 | Collection、Category、共同 Tag 数量、同作者轻量加分构成严格分层关系；同作者不能单独入选 | 通过 |
| 稳定排序 | 关系分相同时按 `published_at DESC, id DESC` 排序，可重复且可测试 | 通过 |
| 原因解释 | 卡片直接展示实际命中的合集、分类、共同标签和同作者原因 | 通过 |
| 范围控制 | 不读取阅读、点赞、收藏或用户行为，不使用热门、AI 相似度、画像、Feed 或个性化推荐 | 通过 |
| 浏览器验收 | 隔离登录态覆盖 0/1/2/4 篇、原因可见、卡片跳转、无权合集不泄露、浅深色、1280px/390px 无溢出和 Console | 通过 |

## 外部环境未验证

- 未连接真实 MySQL 8，因此没有把目标实例上的 `db upgrade` 写成通过；当前只完成 SQLite migration 行为测试和 MySQL dialect DDL 编译。
- 未连接真实 S3-compatible bucket，因此没有把上传、读取、ACL、超时或凭证轮换写成外部验证通过；当前只完成本地私有存储集成和 S3 配置加载。
- 未提供真实 SMTP 账号、可投递域名或 DNS 控制权，因此 STARTTLS 握手、实际到信、退信、垃圾邮件评分以及 SPF/DKIM/DMARC 均待部署环境验证。
- 未提供真实 Redis 服务，因此分布式限流压测仍待部署环境执行。

阶段 21/22/23/24/25/26/27 的仓库内 DoD 已完成；上述外部项是上线门禁，不应被当前本地自动化结果替代。
